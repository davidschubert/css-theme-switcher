on: 
  push:
    branches:
      - master
name: 🚀 Deploy website master on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.1.0
      with:
        server: w00bf3b5.kasserver.com
        username: ${{ secrets.ftp_username }}
        password: ${{ secrets.ftp_password }}
        server-dir: /123production.6co.de/
        local-dir: /Users/david.schubert/Developer/Projekte/htmlcssjs/css-theme-switcher/public/